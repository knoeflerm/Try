<% provide :title, 'All users' %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %> <!-- This iterates over @users and renders partial _user.html.erb -->
</ul>

<%= will_paginate %>